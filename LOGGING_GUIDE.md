# 📝 Логирование в Review Generator

## 📍 Где находятся логи?

**Логи сохраняются в папке пользователя:**
```
C:\Users\[Ваше_Имя]\ReviewGenerator\logs\
```

**Имя файла с датой:**
```
review_generator_2026-01-02.log
```

## 🔍 Что логируется?

### **Парсинг отзывов с сайтов:**
- URL страницы для парсинга
- Статус загрузки страницы
- Какая стратегия используется (schema.org, классы, data-review)
- Количество найденных элементов
- Ошибки обработки каждого элемента
- Полный traceback при краше

### **Генерация отзывов AI:**
- Какая модель используется (perplexity, mistral, deepseek)
- Длина ответа от AI
- Первые 500 символов ответа
- Какой метод парсинга сработал
- Ошибки парсинга с traceback

### **Системные события:**
- Запуск приложения
- Работа с базой данных
- Ошибки импорта/экспорта

## 📖 Как просмотреть логи?

### **Способ 1: Откройте файл напрямую**
1. Откройте проводник
2. Введите в адресную строку: `%USERPROFILE%\ReviewGenerator\logs`
3. Откройте файл с текущей датой

### **Способ 2: Через блокнот**
1. Нажмите Win + R
2. Введите: `%USERPROFILE%\ReviewGenerator\logs`
3. Откройте `.log` файл в блокноте

### **Способ 3: Через VS Code**
1. Откройте VS Code
2. Файл → Открыть файл
3. Перейдите в папку логов
4. Откройте лог файл

## 🎯 Что искать при проблемах?

### **При краше парсинга:**
```
!!! КРАШ ПАРСИНГА СТРАНИЦЫ !!!
URL: https://...
Ошибка: ...
Тип ошибки: ...
Full traceback: ...
```

### **При проблемах с AI:**
```
!!! КРАШ ПАРСИНГА !!!
Ошибка: ...
Тип ошибки: ...
Full traceback: ...
Ответ AI: ...
```

### **При сетевых проблемах:**
```
Ошибка загрузки страницы...
Статус: 404
TimeoutError: ...
```

## 🛠️ Настройка логирования

### **Уровни логирования:**
- `DEBUG` - детальная информация
- `INFO` - основные события
- `WARNING` - предупреждения
- `ERROR` - ошибки
- `CRITICAL` - критические ошибки

### **Формат логов:**
```
2026-01-02 19:45:12 - ReviewGenerator - INFO - === НАЧАЛО ПАРСИНГА СТРАНИЦЫ ===
2026-01-02 19:45:12 - ReviewGenerator - INFO - URL: https://example.com/product
2026-01-02 19:45:13 - ReviewGenerator - INFO - Страница загружена, статус: 200
```

## 🧹 Очистка логов

**Логи накапливаются со временем. Для очистки:**

1. **Удалите старые файлы** - оставьте только последние 2-3 дня
2. **Автоматическая очистка** - можно настроить через планировщик задач

**Скрипт для очистки (очистка старше 7 дней):**
```batch
@echo off
forfiles /p "%USERPROFILE%\ReviewGenerator\logs" /m *.log /d -7 /c "cmd /c del @path"
```

## 📞 Поддержка

**При обращении в поддержку:**
1. **Скопируйте последние 100 строк** из лог файла
2. **Опишите что делали** перед проблемой
3. **Укажите время** когда произошла проблема

**Это поможет быстро найти и исправить проблему!** ✨
